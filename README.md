To run the project:

1. Make sure Docker Desktop is running.
*** Still need to add the key generation script
2. From the project root, run:

   Windows:
   run.bat

   macOS/Linux:
   chmod +x run.sh
   ./run.sh

The script removes old containers for this project, rebuilds the images, and starts all services.